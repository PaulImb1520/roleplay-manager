// Stub de puerto para CharacterRepository.
// Se implementa en el slice S2 (Gestión de personajes).
// Mantenido aquí como `type` placeholder para que la capa de
// aplicación pueda referenciar la forma del contrato desde S0
// sin necesidad de tocar el resto.

export type CharacterRepository = unknown
